name: Daily Brainrot Short

on:
  schedule:
    # Runs twice daily at peak engagement hours: 1:30 PM & 7:30 PM IST (08:00 & 14:00 UTC)
    - cron: "0 8,14 * * *"
  workflow_dispatch:
    inputs:
      style:
        description: "Brainrot style (random if empty)"
        required: false
        default: "random"
        type: choice
        options:
          - random

concurrency:
  group: brainrot-daily
  cancel-in-progress: false

jobs:
  generate-and-upload:
    name: "Generate & upload brainrot short"
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install FFmpeg
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Restore clip cache
        id: cache-clips
        uses: actions/cache/restore@v4
        with:
          path: data/
          key: brainrot-data-${{ github.run_id }}
          restore-keys: |
            brainrot-data-

      - name: Run pipeline
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.CLOUDINARY_API_SECRET }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
          IG_ACCOUNT_ID: ${{ secrets.IG_ACCOUNT_ID }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          IG_USERNAME: ${{ secrets.IG_USERNAME }}
          IG_PASSWORD: ${{ secrets.IG_PASSWORD }}
        run: |
          STYLE="${{ github.event.inputs.style || 'random' }}"
          # Skip download on CI — yt-dlp gets bot-blocked on GitHub runners.
          # Relies on cached clips or fallback clip generation.
          python run_pipeline.py --skip-download --style "$STYLE"

      - name: Save clip cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: data/
          key: brainrot-data-${{ github.run_id }}